    # Python
    __pycache__/
    *.pyc
    *.pyd
    *.pyo
    .Python
    env/
    venv/
    *.env
    .idea/  # IntelliJ IDEA project files
    .vscode/ # VS Code project files
    *.sqlite3
    db.sqlite3
    /media/ # User uploaded media files
    /static/ # Django collected static files (if serving them directly)
    *.log
    .DS_Store # macOS files

    # Node.js / npm
    /node_modules/
    .env.local
    .env.development.local
    .env.test.local
    .env.production.local
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    # dist/ # Common build output for JS
    /build/ # React build output directory
    .cache/
    .eslintcache
    node_modules
node_modules
